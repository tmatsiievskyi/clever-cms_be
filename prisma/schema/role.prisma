
model Role {
    id Int @id @default(autoincrement())

    name String
    description String
    
    policies Policy[]
    users User[]
    units Unit[]

    createdAt DateTime @default(now())
    updatedAt DateTime @default(now())
}